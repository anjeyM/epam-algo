{
  "name": "building-blocks-of-oop-part-1",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "jest",
    "start:dev": "npx nodemon",
    "build": "rimraf ./build && tsc",
    "start:prod": "npm run build && node build/index.js"

  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "jest": "^26.4.2",
    "nodemon": "^3.0.1",
    "prettier": "^2.8.0",
    "rimraf": "^5.0.5",
    "ts-jest": "^26.4.0",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.7"
  },
  "prettier": {
    "printWidth": 120
  }
}
